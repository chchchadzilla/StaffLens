# StaffLens - Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit the .env file to git!

# ===========================================
# REQUIRED - Discord Bot Configuration
# ===========================================

# Your Discord bot token (from Discord Developer Portal)
# Get it: discord.com/developers/applications → Your App → Bot → Reset Token
DISCORD_TOKEN=your_discord_bot_token_here

# Channel ID where interview reports are posted
# Get it: Right-click the channel → Copy Channel ID (requires Developer Mode)
REPORT_CHANNEL_ID=1234567890123456789

# ===========================================
# REQUIRED - API Keys
# ===========================================

# Deepgram API key for speech-to-text transcription
# Get it: console.deepgram.com → API Keys → Create New API Key
DEEPGRAM_API_KEY=your_deepgram_api_key_here

# OpenRouter API key for AI conversations and analysis
# Get it: openrouter.ai → Keys → Create Key
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# AI model to use for conversations and analysis
# Default is Claude 3 Haiku - fast, reliable, and high quality
OPENROUTER_MODEL=anthropic/claude-3-haiku

# ===========================================
# OPTIONAL - Customization
# ===========================================

# Role name that triggers the interview (default: "Applicant")
# Must match EXACTLY (case-sensitive)
APPLICANT_ROLE_NAME=Applicant

# Fit score threshold for "Recommended" status (default: 70)
# Candidates scoring below this are marked as "Not Recommended"
FIT_THRESHOLD=70

# Command prefix for bot commands (default: "!")
COMMAND_PREFIX=!

# ===========================================
# OPTIONAL - Local Analysis Service
# ===========================================

# If you have a local ConversaTrait endpoint, the bot will try it first
# before falling back to OpenRouter (saves money if you self-host)
CONVERSATRAIT_ENDPOINT=http://localhost:9287/api/analyze

# Timeout for local analysis in seconds (default: 30)
LOCAL_ANALYSIS_TIMEOUT=30

# ===========================================
# OPTIONAL - Database
# ===========================================

# Custom database path (default: data/stafflens.db)
# DATABASE_PATH=data/stafflens.db
